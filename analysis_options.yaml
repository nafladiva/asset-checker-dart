include: package:lints/recommended.yaml

analyzer:
  exclude:
    - test/fixtures/**
  language:
    strict-casts: true
    strict-raw-types: true

linter:
  rules:
    - prefer_final_locals
    - unawaited_futures
